# 07-1.4 翻页声音交互层

- 基线：`4e614995`（33 页 V1.0 冻结初稿）
- 范围：仅新增声音交互、音效资源、离线缓存版本及验证工具；页面视觉图片、产品图片和产品内容不变。
- 默认行为：静音。封面右上角提供小型开关，用户主动开启后，仅在实际翻页时播放同一个短促纸张音效。
- 会话状态：使用 `sessionStorage` 保存，仅在当前浏览会话内延续；新会话仍默认静音。
- 音效：`assets/audio/page-turn.wav`，单声道 22.05kHz、约 0.72 秒、31,796 字节。
- 兼容处理：使用原生 HTML Audio、`playsinline`、用户手势解锁及失败静默降级；适用于现代 iOS Safari、Android Chrome 和相应微信 WebView 内核。

## 验证

- 内联脚本及音效生成脚本语法检查通过。
- 390×844 与 360×800 两种手机竖屏尺寸的 Edge/Chromium 自动化测试通过。
- 已验证默认静音、开关状态文案与无障碍属性、开启后翻页触发、会话内刷新保持、关闭后不再触发。
- 已核对 `pages/` 与 `assets/products/` 相对冻结提交的 Git 对象，内容无变化。
- Service Worker 已升级缓存键并预缓存唯一音效资源。

## 回退

完整回退到冻结版：部署或切换至 `4e614995`。

仅撤销本交互层：在后续提交上执行 `git revert <本次提交号>`，无需改动任何冻结页面图片或产品内容。
